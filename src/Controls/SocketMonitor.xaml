<UserControl
    x:Class="OpenFrp.Launcher.Controls.SocketMonitor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behavior="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:Awe.UI.Helper;assembly=Awe.UI"
    xmlns:local="clr-namespace:OpenFrp.Launcher.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:OpenFrp.Launcher.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <FrameworkElement.DataContext>
        <vm:SocketMonitorViewModel />
    </FrameworkElement.DataContext>
    <behavior:Interaction.Triggers>
        <behavior:EventTrigger EventName="Loaded">
            <behavior:InvokeCommandAction Command="{Binding event_CreateStreamingCommand}" />
        </behavior:EventTrigger>
    </behavior:Interaction.Triggers>
    <Grid>
        <StackPanel>
            <TextBlock
                helper:TextOptionHelper.TextAutoHiting="True"
                FontSize="16"
                Text="用户侦测" />
            <ItemsControl />
        </StackPanel>
    </Grid>
</UserControl>
